# macOS
.DS_Store

# Xcode — build output
build/
DerivedData/

# Xcode — per-user state (never share)
xcuserdata/
*.xcuserstate
*.moved-aside

# Swift Package Manager
.build/
.swiftpm/
Packages/*/.build/
Packages/*/.swiftpm/

# Note: Package.resolved is committed at the app level so dependency versions stay pinned across machines.
